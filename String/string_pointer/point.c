#include <stdio.h>

int main()
{
    // char *canChange = "Hello World ";
    // puts(canChange);
    // canChange = "Hello";
    // puts(canChange);

    // pointer string equal not pointer array

    // char canNotChange[] = "Hello World !";
    // puts(canNotChange);
    // // canNotChange = "Hello";
    // puts(canNotChange);
    // return
}