#include <stdio.h>
#include <string.h>

int main()
{
    char str[100];
    char str1[100];

    // printf("Enter string lowercase \n");
    // gets(str);
    // printf("result the lowercase: %s\n", strlwr(str));

    // printf("Enter string uppercase \n");
    // gets(str1);
    // printf("result the uppercase : %s\n", strupr(str1));

    return 0;
}